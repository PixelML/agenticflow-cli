---
phase: 02-ishi-integration-more-packs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/SKILL.md
  - /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/reference/cli-setup.md
  - /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/reference/workflow/connections.md
  - /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/reference/packs.md
autonomous: true
requirements: [ISHI-02]
must_haves:
  truths:
    - "Ishi LLM can read SKILL.md and know how to bootstrap, create agents, run workflows, use packs, handle connections"
    - "SKILL.md lists available packs with descriptions so LLM can recommend the right pack"
    - "Skill teaches auto-install via npx so no pre-install is required"
    - "Skill documents action workflow pattern (LLM -> mcp_run_action) from Phase 1"
    - "Skill documents connection pre-flight and _links.mcp guidance"
  artifacts:
    - path: "/Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/SKILL.md"
      provides: "Updated navigation table with pack catalog section and Phase 1 feature rows"
      contains: "Pack Catalog"
    - path: "/Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/reference/packs.md"
      provides: "Pack system reference: install, validate, run entrypoints, company.yaml format"
      contains: "af pack validate"
    - path: "/Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/reference/workflow/connections.md"
      provides: "Updated with action workflow pattern and _links.mcp guidance"
      contains: "mcp_run_action"
    - path: "/Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/reference/cli-setup.md"
      provides: "Updated with bootstrap --json full output shape and _links"
      contains: "agenticflow.bootstrap.v1"
  key_links:
    - from: "SKILL.md"
      to: "reference/packs.md"
      via: "Quick Navigation table link"
      pattern: "packs.md"
    - from: "SKILL.md"
      to: "reference/workflow/connections.md"
      via: "Quick Navigation table link"
      pattern: "connections.md"
---

<objective>
Update the AgenticFlow skill for Ishi with Phase 1 features and pack catalog.

Purpose: Enable Ishi LLM to use the full AgenticFlow CLI feature set including action workflows, connection pre-flight, pack system, and bootstrap _links. Per D-04, the skill is a full playbook. Per D-07, it must include all Phase 1 features.

Output: Updated SKILL.md, new packs.md reference, updated connections.md and cli-setup.md in the agenticflow-skill repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ishi-integration-more-packs/02-CONTEXT.md
@.planning/phases/02-ishi-integration-more-packs/02-RESEARCH.md

<interfaces>
<!-- Key structures the executor needs from existing code -->

From agenticflow-skill/SKILL.md (current structure):
- Frontmatter: name: agenticflow-skills, description: ..., license: MIT
- Sections: Quick Navigation table, Workflow, Agent, Workforce, Glossary, Quality Gate
- Navigation table has columns: Topic | When to Use | Reference

From packages/cli/src/cli/main.ts (bootstrap output shape, verified):
```json
{
  "schema": "agenticflow.bootstrap.v1",
  "auth": { "authenticated": true, "health": true, "workspace_id": "<uuid>", "project_id": "<uuid>" },
  "agents": [{ "id": "...", "name": "...", "model": "..." }],
  "commands": { "run_agent": "af agent run ...", "create_agent": "af agent create ...", "deploy_to_paperclip": "af paperclip init ..." },
  "models": ["agenticflow/gemma-4-31b-it", ...],
  "blueprints": [{ "id": "...", "name": "...", "agents": 5 }],
  "_links": { "workspace": "https://agenticflow.ai/...", "connections": "...", "mcp": "...", "settings": "...", "datasets": "..." }
}
```

From packages/cli/src/cli/pack.ts (PackManifest interface):
```yaml
apiVersion: pixelml.ai/pack/v1
kind: Pack
name: <pack-name>
version: 1.0.0
entrypoints: [{ id, workflow, mode, description }]
connections: [{ category, name, required }]
company: company.yaml
```

From agent-skills/packs/amazon-seller-pack/workflows/post-review-to-gbp.workflow.json:
- Action workflow pattern: LLM node (node_type_name: "llm") -> mcp_run_action node (node_type_name: "mcp_run_action")
- Variable interpolation: ${node-name.generated_text}
- Input params: {{variable}} for user input, ${node.field} for inter-node wiring
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create packs.md reference and update connections.md with action workflows</name>
  <files>
    /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/reference/packs.md
    /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/reference/workflow/connections.md
  </files>
  <read_first>
    /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/reference/workflow/connections.md
    /Users/sean/WIP/Antigravity-Workspace/agent-skills/packs/amazon-seller-pack/pack.yaml
    /Users/sean/WIP/Antigravity-Workspace/agent-skills/packs/amazon-seller-pack/company.yaml
    /Users/sean/WIP/Antigravity-Workspace/agent-skills/packs/amazon-seller-pack/workflows/post-review-to-gbp.workflow.json
  </read_first>
  <action>
Create NEW file `reference/packs.md` with the following content:

**Section 1: "Pack System"** — explain packs are pre-built business agent kits with company.yaml + workflows + skills.

**Section 2: "Available Packs"** (per D-05 Pack Catalog):
| Pack | Business Type | Agents | Key Workflows | Use Case |
|------|--------------|--------|---------------|----------|
| amazon-seller-pack | E-commerce | 5 (Listing Specialist, PPC Manager, Competitor Analyst, Customer Support, Pricing Strategist) | product-launch, competitor-scrape, listing-audit, review-scrape-respond, post-review-to-gbp | Amazon Singapore sellers |
| tutor-pack | Education | 5 (Curriculum Designer, Quiz Creator, Progress Tracker, Parent Communicator, Business Manager) | post-lesson-summary, generate-quiz | Tutors and tutoring businesses |
| freelancer-pack | Services | 5 (Scope Writer, Invoice Generator, Client Researcher, Communication Agent, Biz Dev Manager) | send-invoice, client-status-update | Freelancers and consultants |

**Section 3: "Installing a Pack"**:
```bash
af pack install PixelML/agent-skills/packs/<pack-name> --json
af pack validate ./<pack-name> --json
```

**Section 4: "Running Pack Entrypoints"**:
```bash
af pack run --pack <pack-name> --entrypoint <entrypoint-id> --input @input.json --json
```

**Section 5: "company.yaml Blueprint Format"** — show the full schema with example:
```yaml
apiVersion: pixelml.ai/company/v1
kind: CompanyBlueprint
name: <Name>
description: <description>
model: agenticflow/gemma-4-31b-it
budget_monthly_cents: 100000
agents:
  - name: <Agent Name>
    role: cmo | engineer | researcher | general | ceo
    system_prompt: |
      <domain-specific prompt>
starter_tasks:
  - title: <task title>
    assignee_role: <role>
    priority: high | medium | low
    description: |
      <specific task description>
```

**Section 6: "Deploying via Paperclip"** — show the deployment commands:
```bash
af paperclip init --blueprint <blueprint-id> --json
af gateway serve --channels paperclip &
af paperclip connect --json
```

All CLI examples MUST use `--json` flag. Every section that returns output must include "After running, present `_links.*` URLs to the user."

***

UPDATE `reference/workflow/connections.md` — ADD these sections at the end (keep all existing content):

**New Section: "Action Workflows (LLM -> mcp_run_action)"**:
- Explain the two-node pattern: LLM node drafts content, mcp_run_action node posts it
- Show the workflow JSON structure:
```json
{
  "nodes": [
    { "name": "draft-response", "node_type_name": "llm", "input_config": { "model": "agenticflow/gemma-4-31b-it", "system_message": "...", "human_message": "{{input}}" } },
    { "name": "post-action", "node_type_name": "mcp_run_action", "input_config": { "action": "<service>-<action>", "input_params": { "content": "${draft-response.generated_text}" } } }
  ]
}
```
- Variable interpolation: `{{variable}}` for user input, `${node-name.field}` for inter-node wiring

**New Section: "Connection Pre-Flight Check"**:
- Before running action workflows, check connections are available:
```bash
af connections list --limit 200 --json
```
- If a required MCP connection is missing, present `_links.mcp` URL from `af bootstrap --json` output:
  `"mcp": "https://agenticflow.ai/workspace/<id>/settings/mcp"`
- Tell user: "Add the required MCP connection at this URL, then retry."

**New Section: "Missing Connection Error Handling"**:
- When `af workflow run` fails with connection error, the CLI shows `_links.mcp`
- Extract that URL and present to user with instructions to add the MCP service
  </action>
  <verify>
    <automated>grep -c "mcp_run_action" /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/reference/workflow/connections.md && grep -c "af pack validate" /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/reference/packs.md && grep -c "company.yaml" /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/reference/packs.md</automated>
  </verify>
  <acceptance_criteria>
    - /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/reference/packs.md exists and contains "af pack validate"
    - packs.md contains "amazon-seller-pack" and "tutor-pack" and "freelancer-pack" in Available Packs table
    - packs.md contains "apiVersion: pixelml.ai/company/v1" in company.yaml format section
    - packs.md contains "af pack install" and "af pack run" and "--json"
    - packs.md contains "af paperclip init --blueprint" in deployment section
    - connections.md contains "mcp_run_action" in new action workflows section
    - connections.md contains "draft-response.generated_text" showing inter-node variable wiring
    - connections.md contains "_links.mcp" in connection pre-flight section
    - connections.md contains "af connections list --limit 200 --json"
    - All existing content in connections.md is preserved (still contains "Connection Providers" section)
  </acceptance_criteria>
  <done>New packs.md reference file created with pack catalog, install/validate/run commands, company.yaml schema, and deployment instructions. connections.md updated with action workflow pattern, connection pre-flight check, and missing connection error handling.</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md navigation and cli-setup.md bootstrap output</name>
  <files>
    /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/SKILL.md
    /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/reference/cli-setup.md
  </files>
  <read_first>
    /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/SKILL.md
    /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/reference/cli-setup.md
  </read_first>
  <action>
UPDATE `SKILL.md`:

1. Keep the existing frontmatter exactly as-is:
```yaml
***
name: agenticflow-skills
description: Comprehensive guide for building AI workflows, agents, and workforce systems with AgenticFlow. Use when designing workflows with various node types, configuring single agents, or orchestrating workforce collaboration patterns.
license: MIT
***
```

2. Add these rows to the Quick Navigation table (insert after the "CLI Setup" row):
```
| **Packs** | Using pre-built business agent packs | [reference/packs.md](./reference/packs.md) |
| **Action Workflows** | LLM -> MCP action chains (post, send, update) | [workflow/connections.md](./reference/workflow/connections.md#action-workflows-llm---mcp_run_action) |
| **Connections** | MCP connection pre-flight, missing connection handling | [workflow/connections.md](./reference/workflow/connections.md#connection-pre-flight-check) |
```

3. Add a new "Packs" section after the "Agent" section and before "Workforce":
```markdown
***

## Packs

Packs are pre-built business agent kits — each contains a company blueprint (agents + starter tasks), action workflows, and domain-specific skills.

**To browse available packs and install one, load:** [reference/packs.md](./reference/packs.md)

### Available Packs

| Pack | Best For |
|------|----------|
| `amazon-seller-pack` | Amazon e-commerce sellers (Singapore market) |
| `tutor-pack` | Tutoring businesses, education professionals |
| `freelancer-pack` | Freelancers, consultants, independent professionals |

### Quick Start

```bash
# Install a pack
af pack install PixelML/agent-skills/packs/tutor-pack --json

# Validate structure
af pack validate ./tutor-pack --json

# Deploy agents from pack blueprint
af paperclip init --blueprint tutor --json
```
```

4. Add an "Auto-Install" section at the top of the doc, right after the title "# AgenticFlow Skills" and before "## Quick Navigation" (per D-01 shell-out, D-02 auto-install):
```markdown
## First-Time Setup (for AI agents)

Check if the AgenticFlow CLI is available. If not, use `npx` to run it without prior installation:

```bash
# Check if af CLI is installed
command -v af >/dev/null 2>&1 && echo "af available" || echo "af not found"

# If not found, use npx (no install required):
npx @pixelml/agenticflow-cli bootstrap --json

# Or install globally for faster repeat runs:
npm install -g @pixelml/agenticflow-cli
af bootstrap --json
```

Always use `--json` on all commands for structured output that you can parse.

After bootstrapping, extract `_links` from the JSON output and present web UI URLs to the user.
```

***

UPDATE `reference/cli-setup.md`:

Add a new section "## Bootstrap Output Shape" after the existing "## Authentication" section. Include the full `af bootstrap --json` response shape:

```markdown
## Bootstrap Output Shape

`af bootstrap --json` returns the full workspace context — use this as the entry point for all operations:

```json
{
  "schema": "agenticflow.bootstrap.v1",
  "auth": {
    "authenticated": true,
    "health": true,
    "workspace_id": "<uuid>",
    "project_id": "<uuid>"
  },
  "agents": [{ "id": "...", "name": "...", "model": "..." }],
  "schemas": ["agent", "workflow", "..."],
  "commands": {
    "run_agent": "af agent run --agent-id <id> --message <msg> --json",
    "create_agent": "af agent create --body <json> --dry-run --json",
    "deploy_to_paperclip": "af paperclip init --blueprint <id> --json"
  },
  "models": ["agenticflow/gemma-4-31b-it", "agenticflow/gemini-2.0-flash"],
  "blueprints": [{ "id": "...", "name": "...", "agents": 5 }],
  "playbooks": ["first-touch"],
  "whats_new": { "version": "1.3.1", "highlights": ["..."] },
  "_links": {
    "workspace": "https://agenticflow.ai/workspace/<id>",
    "connections": "https://agenticflow.ai/workspace/<id>/settings/connections",
    "mcp": "https://agenticflow.ai/workspace/<id>/settings/mcp",
    "settings": "https://agenticflow.ai/workspace/<id>/settings",
    "datasets": "https://agenticflow.ai/workspace/<id>/datasets"
  }
}
```

**Key fields:**
- `auth.authenticated` — check this first; if false, run `af login` or set `AGENTICFLOW_API_KEY`
- `agents[]` — existing agents in workspace; use their IDs for `af agent run`
- `commands` — copy-paste ready commands for common operations
- `_links` — web UI URLs to present to the user after each operation
- `blueprints[]` — available company blueprints for `af paperclip init --blueprint <id>`
```

Also add a note in the Authentication section about env var auth for AI agents:
```markdown
> **For AI agents (Ishi, Claude):** Prefer `AGENTICFLOW_API_KEY` env var over interactive `af login`. Check auth status with `af whoami --json` first. If unauthenticated, guide the user to run `af login` in their terminal or set the env var.
```
  </action>
  <verify>
    <automated>grep -c "Packs" /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/SKILL.md && grep -c "packs.md" /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/SKILL.md && grep -c "agenticflow.bootstrap.v1" /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/reference/cli-setup.md && grep -c "npx @pixelml/agenticflow-cli" /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md frontmatter still has "name: agenticflow-skills" (unchanged)
    - SKILL.md Quick Navigation table contains row with "Packs" linking to "reference/packs.md"
    - SKILL.md Quick Navigation table contains row with "Action Workflows" linking to "workflow/connections.md"
    - SKILL.md contains "## Packs" section with Available Packs table listing amazon-seller-pack, tutor-pack, freelancer-pack
    - SKILL.md contains "## First-Time Setup" section with "npx @pixelml/agenticflow-cli bootstrap --json"
    - SKILL.md contains "command -v af" auto-install detection pattern
    - SKILL.md contains "--json" in Quick Start commands
    - cli-setup.md contains "agenticflow.bootstrap.v1" in bootstrap output shape section
    - cli-setup.md contains "_links" with workspace, connections, mcp, settings, datasets URLs
    - cli-setup.md contains "AGENTICFLOW_API_KEY" env var note for AI agents
  </acceptance_criteria>
  <done>SKILL.md updated with auto-install section, pack catalog navigation, pack section with all 3 packs listed. cli-setup.md updated with full bootstrap --json output shape and AI agent auth guidance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ishi LLM -> af CLI | Ishi executes af commands via Bash tool; CLI trusts shell environment |
| af CLI -> AgenticFlow API | CLI sends API key over HTTPS to api.agenticflow.ai |
| Skill docs -> LLM behavior | Skill content shapes what commands the LLM runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | SKILL.md / reference docs | mitigate | All examples use env var pattern (AGENTICFLOW_API_KEY), never inline API keys. Auth section explicitly says "never hardcode keys." |
| T-02-02 | Elevation of Privilege | Skill shell-out instructions | mitigate | Skill teaches only documented af CLI commands with --json flag. No arbitrary shell execution. Commands are read-only (list, get) or scoped (create agent in user's workspace). |
| T-02-03 | Tampering | Pack manifest content | accept | Pack YAML and workflow JSON are static files authored by trusted pack maintainers. No user interpolation in system_prompt at pack install time. |
</threat_model>

<verification>
1. `grep -c "name: agenticflow-skills" /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/SKILL.md` returns 1 (frontmatter preserved)
2. `grep -c "packs.md" /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/SKILL.md` returns >= 2 (navigation + section link)
3. `grep -c "mcp_run_action" /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/reference/workflow/connections.md` returns >= 2
4. `test -f /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/reference/packs.md` exits 0
5. `grep -c "af pack validate" /Users/sean/WIP/Antigravity-Workspace/agenticflow-skill/reference/packs.md` returns >= 1
</verification>

<success_criteria>
AgenticFlow skill updated with complete Phase 1 feature documentation. An Ishi LLM reading SKILL.md can discover all capabilities (packs, action workflows, connections, bootstrap) via the navigation table and follow reference links to get full command syntax with --json output shapes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-ishi-integration-more-packs/02-01-SUMMARY.md`
</output>
