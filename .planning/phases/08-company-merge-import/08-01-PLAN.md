---
phase: 08-company-merge-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/cli/company-io.ts
  - packages/cli/tests/company-io.test.ts
autonomous: true
requirements: [ECO-08]
must_haves:
  truths:
    - "mergeImportCompany() classifies each local agent as new/modified/in_sync and reports remote_only without deleting"
    - "strategy=local overwrites modified agents with file values"
    - "strategy=remote skips modified agents (live wins)"
    - "strategy=skip skips modified agents but still creates new agents"
    - "dryRun=true makes zero create/update calls"
    - "in_sync agents are not written (no no-op update)"
  artifacts:
    - path: "packages/cli/src/cli/company-io.ts"
      provides: "ConflictStrategy type, MergeAgentEntry, CompanyMergeResult, CompanyMergeDryRunResult, mergeImportCompany()"
      contains: "export async function mergeImportCompany"
    - path: "packages/cli/tests/company-io.test.ts"
      provides: "describe('mergeImportCompany') unit test block"
      contains: "describe(\"mergeImportCompany\""
  key_links:
    - from: "mergeImportCompany"
      to: "client.agents.list / create / update"
      via: "single list call + per-strategy writes"
      pattern: "client\\.agents\\.(list|create|update)"
---

<objective>
Add `mergeImportCompany()` to `company-io.ts` as a pure async function that classifies local agents against the live workspace, then applies writes per a `ConflictStrategy` ("local" | "remote" | "skip"). Supports `dryRun` mode that returns the resolved plan without writing. Reuses Phase 7 primitives (`changedFields`, `extractAgentsFromListResponse`) and mirrors `diffCompany()` classification logic inline (single list call — no double fetch). Adds full unit test coverage in `company-io.test.ts`.

Purpose: Foundation layer for ECO-08. CLI wiring (Plan 02) consumes this function.
Output: New exported function + types in company-io.ts; new `describe("mergeImportCompany")` block in company-io.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-company-merge-import/08-RESEARCH.md
@.planning/phases/07-company-diff/07-01-SUMMARY.md
@packages/cli/src/cli/company-io.ts
@packages/cli/tests/company-io.test.ts

<interfaces>
From packages/cli/src/cli/company-io.ts (already exported):

```typescript
export type DiffAgentStatus = "new" | "modified" | "remote_only" | "in_sync";
export interface DiffAgentEntry { name: string; status: DiffAgentStatus; changed_fields: string[]; }
export interface CompanyExportSchema { schema: "agenticflow.company.export.v1"; agents: CompanyExportAgentEntry[]; /* ... */ }
export class CompanyIOError extends Error { code: string; /* ... */ }
export function changedFields(local: CompanyExportAgentEntry, existing: Record<string, unknown>): string[];
export function extractAgentsFromListResponse(raw: unknown): Record<string, unknown>[];
```

Reference signature pattern (mirror this structure):
```typescript
export async function diffCompany(
  client: AgenticFlowClient,
  localSchema: CompanyExportSchema,
): Promise<CompanyDiffResult>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ConflictStrategy + merge result types to company-io.ts</name>
  <files>packages/cli/src/cli/company-io.ts</files>
  <behavior>
    - Exports `ConflictStrategy = "local" | "remote" | "skip"`
    - Exports `MergeAgentEntry` with fields: name, status (DiffAgentStatus), changed_fields, resolution ("created" | "updated" | "skipped" | "no_change" | "remote_only")
    - Exports `CompanyMergeResult` with schema "agenticflow.company.merge.v1", conflict_strategy, summary { created, updated, skipped, no_change, remote_only }, agents
    - Exports `CompanyMergeDryRunResult` with schema "agenticflow.company.merge.dry-run.v1", conflict_strategy, conflicts, would_create, would_update, would_skip
    - File still type-checks (`pnpm --filter @pixelml/agenticflow-cli typecheck` or `tsc --noEmit`)
  </behavior>
  <action>
    Append a new section after the diffCompany block (after line ~366) titled `// mergeImportCompany — Plan 08-01 (ECO-08)`. Add the type definitions exactly per RESEARCH.md "Code Examples > Type definitions to add in company-io.ts". Do NOT add the function body yet — types only. This isolates the type surface for downstream test compilation in Task 2.
  </action>
  <verify>
    <automated>cd packages/cli && npx tsc --noEmit</automated>
  </verify>
  <done>Types exported, file type-checks, no other behavior changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing unit tests for mergeImportCompany</name>
  <files>packages/cli/tests/company-io.test.ts</files>
  <behavior>
    Add `describe("mergeImportCompany", () => { ... })` block with these tests (all failing initially because the function does not exist):
    - "creates new agents that exist in file but not in workspace"
    - "skips in_sync agents (no update call)"
    - "strategy=local overwrites modified agents with file values (calls update)"
    - "strategy=remote skips modified agents (no update call)"
    - "strategy=skip skips modified agents but still creates new agents"
    - "remote_only agents are reported but never deleted (no delete call)"
    - "dry-run makes zero create/update calls and returns CompanyMergeDryRunResult"
    - "throws CompanyIOError on schema_version_mismatch"
    - "summary counts match resolutions"
    Use existing `makeMockClient()` / `makeImportClient()` patterns from the file. Mock `client.agents.list/create/update` and assert call counts. Use `return void fail()` pattern is N/A here (not Commander handler) — standard `expect().toThrow()` is fine.
  </behavior>
  <action>
    Read existing `describe("diffCompany"` and `describe("importCompany"` blocks for the mock client pattern. Add a new describe block at the end of the file. Import `mergeImportCompany`, `ConflictStrategy`, `CompanyMergeResult`, `CompanyMergeDryRunResult` from `../src/cli/company-io.js`. Build minimal `CompanyExportSchema` fixtures inline. Run tests — they MUST fail with "mergeImportCompany is not a function" or similar (RED phase).
  </action>
  <verify>
    <automated>cd packages/cli && npx vitest run tests/company-io.test.ts -t "mergeImportCompany" 2>&1 | grep -E "(FAIL|fail)" | head -5</automated>
  </verify>
  <done>Test block exists with all 9 cases; tests fail because function not yet implemented (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement mergeImportCompany() to make tests green</name>
  <files>packages/cli/src/cli/company-io.ts</files>
  <behavior>
    - Schema version guard throws CompanyIOError("schema_version_mismatch") for non-v1 schemas
    - Calls `client.agents.list({ projectId, limit: 1000 })` exactly ONCE (not twice — Pitfall 1)
    - Builds existingByName map; classifies each local agent inline using `changedFields()` (no separate diffCompany call)
    - Collects remote_only agents (existsInWorkspace && !inLocal) → reported only, never deleted
    - If `dryRun === true`: returns CompanyMergeDryRunResult with conflicts (modified agents only), would_create (new agents), would_update (modified resolved as "update" by strategy), would_skip (modified resolved as "skip" by strategy). ZERO writes.
    - Otherwise applies writes:
      - new agents → `client.agents.create()` → resolution "created"
      - in_sync agents → no call → resolution "no_change"
      - modified agents per strategy:
        - "local" → `client.agents.update()` with file values → resolution "updated"
        - "remote" → no call → resolution "skipped"
        - "skip" → no call → resolution "skipped"
      - remote_only → no call → resolution "remote_only"
    - Returns CompanyMergeResult with summary counts matching resolutions
    - Agents array sorted alphabetically (consistency with diffCompany)
  </behavior>
  <action>
    Implement the function per the skeleton in RESEARCH.md "Code Examples > mergeImportCompany() skeleton". Mirror the structure of `diffCompany()` for the classification phase (lines 293-366), then add the strategy-driven write phase. Use the same `extractAgentsFromListResponse()` and `changedFields()` primitives. Use the existing `importCompany()` (lines 166-264) as the reference for how to call `client.agents.create()` and `client.agents.update()` — same projectId handling, same payload shape. Run tests until GREEN.
  </action>
  <verify>
    <automated>cd packages/cli && npx vitest run tests/company-io.test.ts -t "mergeImportCompany"</automated>
  </verify>
  <done>All 9 unit tests pass. `npx tsc --noEmit` clean. No double list() calls. remote_only never triggers a delete (verified by mock).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local YAML file → mergeImportCompany | parsed schema enters function (validation upstream in CLI handler) |
| mergeImportCompany → SDK | create/update calls go to live workspace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | mergeImportCompany schema input | mitigate | Schema version guard throws CompanyIOError("schema_version_mismatch") for non-v1 — mirrors importCompany / diffCompany |
| T-08-02 | Tampering / Data Loss | remote_only agent handling | mitigate | Function MUST NOT call delete on any agent. Unit test asserts `client.agents.delete` is never called (or never even mocked). Resolution field "remote_only" only. |
| T-08-03 | Information Disclosure | summary counts | accept | Summary exposes only agent names already known to the caller; no PII expansion |
| T-08-04 | Tampering / Silent overwrite | dryRun flag honored | mitigate | dryRun=true short-circuits before write phase. Unit test asserts create/update mocks called 0 times when dryRun=true |
</threat_model>

<verification>
- `cd packages/cli && npx vitest run tests/company-io.test.ts` → all tests pass (existing + new)
- `cd packages/cli && npx tsc --noEmit` → clean
- Single `client.agents.list` call per `mergeImportCompany` invocation (assert in test)
- `client.agents.delete` never called in any test path
</verification>

<success_criteria>
- mergeImportCompany() exists, exported, type-checks
- 9 unit tests pass
- Function makes ZERO writes when dryRun=true
- remote_only agents never deleted
- Strategy semantics match the table in RESEARCH.md Pattern 1
</success_criteria>

<output>
Create `.planning/phases/08-company-merge-import/08-01-SUMMARY.md` after completion documenting:
- Final function signature
- Test count and pass status
- Confirmation of single-list-call invariant
- Any deviations from plan
</output>
</content>
</invoke>